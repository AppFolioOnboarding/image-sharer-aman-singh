class FeedbackStore {
  name = null;
  comment = null;

  setName = (name) => {
    this.name = name;
  };

  setComment = (comment) => {
    this.comment = comment;
  };
}

export default FeedbackStore;
